// @ts-nocheck
// Module: ready-dialog/loading-overlay -- DELETED in Wave 3 Ship 8 (v1.418).
// All loading-overlay UX is gone with the loading-gate machinery; the player snaps directly to
// the deploy screen on first join and on team-swap. This file remains as a stub so the import
// chain survives the bumpVersion regen, but the prebuild script may strip it on next pass.
