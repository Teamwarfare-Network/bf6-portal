# Phase 1 Validator Capability Matrix

Last updated: 2026-03-01  
Requirement format:
`check id -> runtime-observable|human/config -> required symbol(s) -> supported/unverified -> runtime diagnostic behavior`

| Check ID                                                                                          | Runtime-observable or human/config | Required symbol(s)                                              | Supported/unverified                               | Runtime diagnostic behavior                                                                                                |
| ------------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------------------------------------------------- | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `VAL-CAP-001` Capture-point object identity mapping key exists                                    | Runtime-observable + config        | `mod.GetObjId`                                                  | Supported                                          | If runtime capture-point ObjId has no config mapping, use safe no-op for ticket/KPI mutation and emit debug/admin warning. |
| `VAL-CAP-002` Capture ownership read availability                                                 | Runtime-observable                 | `mod.GetCurrentOwnerTeam`                                       | Supported (symbol), runtime probe pending Phase 2A | If read path fails at runtime, downgrade to available owner-event consequences and record diagnostic.                      |
| `VAL-CAP-003` Capture progress read availability                                                  | Runtime-observable                 | `mod.GetCaptureProgress`                                        | Supported (symbol), runtime probe pending Phase 2A | If unavailable, do not synthesize script progress math; operate owner-only consequences + diagnostics.                     |
| `VAL-CAP-004` Owner-progress team read availability                                               | Runtime-observable                 | `mod.GetOwnerProgressTeam`                                      | Supported (symbol), runtime probe pending Phase 2A | If unavailable, keep owner-only accounting and mark reduced-fidelity diagnostics.                                          |
| `VAL-CAP-005` Previous-owner read availability                                                    | Runtime-observable                 | `mod.GetPreviousOwnerTeam`                                      | Supported (symbol), runtime probe pending Phase 2A | If unavailable, infer transitions only from available event edges; no invented reads.                                      |
| `VAL-CAP-006` On-point player list availability                                                   | Runtime-observable                 | `mod.GetPlayersOnPoint`                                         | Supported (symbol), runtime probe pending Phase 2A | If unavailable, defer multi-player capture credit path and keep owner-only projection.                                     |
| `VAL-CAP-007` Runtime object-type introspection for arbitrary map object classes                  | Runtime-observable                 | `TBD (no verified generic type-introspection symbol committed)` | Unverified                                         | Validator must not assume unsupported type checks; rely on ObjId + config ownership and explicit diagnostics.              |
| `VAL-CAP-008` Map schema contains required conquest ObjId groups                                  | Human/config                       | `MapConfig` schema in `src/config/types.ts`                     | Unverified for conquest groups in current baseline | Warn in diagnostics and block phase signoff until schema migration artifacts are complete.                                 |
| `VAL-CAP-009` Phase 7 spawn-set completeness (`teamSpawnSets`, `flagSpawnSets`, `fallbackSpawns`) | Human/config                       | Conquest map schema fields (future)                             | Deferred until Phase 7                             | Pre-Phase 7: diagnostic note only. Phase 7+: strict validation with explicit errors and deny path where required.          |
| `VAL-CAP-010` Runtime warning channel for validator failures                                      | Runtime-observable                 | `mod.DisplayHighlightedWorldLogMessage`                         | Supported                                          | Emit highlighted debug/admin warnings; do not hard-fail mode start in Phase 1.                                             |
