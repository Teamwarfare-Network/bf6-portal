# TWL Conquest Optimization Analysis

Last updated: v1.500 (2026-05-09) — **A12 + A13 SHIPPED clean (long-match audit punchlist closed). Postbuild non-ASCII guardrail added.** Root cause of v1.498 silent-load failure identified: a single em-dash (U+2014) in an inline trailing comment after the `lockerSlotToggle` delete survived postbuild's comment-strip pass (which only removes line-leading `//` comments, not inline ones), and the Portal sandbox SILENTLY rejects scripts containing any non-ASCII byte (no console error, no event handlers register). v1.500 re-applies A12 + A13 with: (a) no inline comment on the lockerSlotToggle delete; (b) pure-ASCII full-line docblocks above each new helper; (c) new `scripts/postbuild.js` step 11.5 hard-fails the build with `process.exit(1)` if any non-ASCII byte is found in `dist/bundle.ts`, printing the offending line + character + code point. Documented in AGENTS.md as new section "Non-ASCII Characters Are Banned in dist/bundle.ts". Bundle 926,137 / 1,048,576 (+563 B from v1.499; 11.67% headroom; -137 B vs broken v1.498 due to dropped inline comment). **All 5 confirmed long-match-audit leaks (L1-L5) now plugged for real.** v1.499 (2026-05-09) — REVERT v1.498. v1.497 (2026-05-09) — **Tier S3 SHIPPED.** `updateVehicleDeployTimerHudForAllPlayers` body replaced with a 200ms-debouncing coalescer at [`vehicles/deploy-timer-ui.ts:1837`](../src/vehicles/deploy-timer-ui.ts#L1837). 26 fire-and-forget broadcast call sites now collapse into one per 200ms window via `Timers.setTimeout` (mirrors combat HUD throttle interval). Each call flips `vehicleDeployTimerHudDirty=true`; first call within an idle window schedules the drain; subsequent calls inside the window are O(1) flag-flips. At drain time the flag is consumed and `forEachValidPlayer` runs once with the latest state (render plan computed at drain time, not flag-set time). Per-player path (`refreshVehicleDeployTimersForPlayerPreservingVisibility` + `lastRenderSignature`) unchanged. Bundle 925,574 / 1,048,576 (+825 B from v1.496; 11.73% headroom). Game-mode init clears any pending timer via new `clearVehicleDeployTimerHudCoalesceTimer` helper called from [`index/game-mode.ts:55`](../src/index/game-mode.ts#L55) reset block; `Timers.clearTimeout` is idempotent. **Estimated CPU win:** ~5–10× fewer vehicle-HUD broadcasts in active 8–10p combat (1,280–3,840 slot-state computes/sec → ≤1,280/sec). MP playtest with diagnostic toggles is the next gate to confirm. Plan archive: [`5.09.26_conquest_S3_vehicle_deploy_timer_throttle_plan.md`](./5.09.26_conquest_S3_vehicle_deploy_timer_throttle_plan.md). v1.496 (2026-05-09) — **Dead-code removal supersedes A11 (v1.495).** Verified `vehicleSpawnBaseTeamByObjId` had **zero read sites** across `src/` — the cache was entirely write-only. v1.496 removes the declaration in [`config/runtime.ts`](../src/config/runtime.ts), the spawn-side write in [`index/vehicle-events.ts`](../src/index/vehicle-events.ts), the v1.495 destroy-side delete (also in vehicle-events.ts), the `clearSpawnBaseTeamCache` helper in [`vehicles/registration.ts`](../src/vehicles/registration.ts), and its lone caller in [`index/game-mode.ts`](../src/index/game-mode.ts). Bundle 924,749 / 1,048,576 (−361 B from v1.495; 11.81% headroom). Lesson recorded for future audits: verify zero-reader status before classifying a record as a leak — A11 was incorrectly framed as a Tier A leak fix when the underlying cache was Tier C dead code. **Within-match scope clarification (user, 2026-05-09):** the script restarts at match end and the v1.491 spike manifests *within* a single match — so multi-match restart cadence is NOT a valid mitigation. Within-match accumulation must be addressed via either intra-match resets, engine-surface reduction (capture-sound queue, world-log buffer, area-trigger churn), or a Tier S burst whose trigger frequency rises with match state. v1.495 (2026-05-09) — A11 shipped (now superseded by v1.496 dead-code removal). v1.494 (2026-05-09) — **Diagnostic admin toggle bundle shipped (v1.492+v1.493+v1.494).** `FEATURE_ADMIN_PANEL` and `FEATURE_POSITION_DEBUG` re-enabled but admin-pid scoped (only the claimed admin sees / interacts; existing `updateAdminPanelActionCountForAllPlayers` N-player broadcast collapsed to single-pid update). Three new admin diagnostic actions wire into the per-frame budget bisection: **Toggle Combat HUD** (off mode short-circuits the per-player VM-build at [`pipeline.ts:106-115`](../src/ui/conquest/hud-core/pipeline.ts#L106-L115) — directly addresses S1/S2 hot path), **Toggle Vehicle Overlay** (refined gate at [`deploy-timer-ui.ts`](../src/vehicles/deploy-timer-ui.ts) hides only when player is walking around — preserves spawn flows; addresses S3), and **Toggle Spectator** (gates `isSpectatorAvailableForActiveMap`; kicks active spec on disable). All three flags are global server-side; position-debug is admin-only. New `Admin.onAdminPidChanged` chokepoint cleanly tears down prior admin's panel + counter widget + position-debug overlay loop on handoff. Plan archives: [`5.08.26_conquest_admin_panel_diagnostic_toggles_plan.md`](./5.08.26_conquest_admin_panel_diagnostic_toggles_plan.md), [`5.09.26_conquest_admin_panel_v2_adjustments_plan.md`](./5.09.26_conquest_admin_panel_v2_adjustments_plan.md). v1.493 dropped Toggle Perf Diag (broken profiler) and Toggle Deploy Timers Visible (superseded by global Toggle Vehicle Overlay), renamed Ground Deploy All → Force Spawn Vehicles (Vanilla Only). v1.494 fixed Combat HUD pre-game no-op (now force-applies via `updateConquestCombatHudForAllPlayers(true)`) and refined Vehicle Overlay gate to `(deployed && !inLiveMenu)` — preserves spawn menu access. Bundle 925,049 / 1,048,576 (11.78% headroom; +59,335 bytes from v1.491 due to ~46K of admin-panel + position-debug feature code re-bundled). **The diagnostic toggles are a TOOL, not a fix.** They let the admin manually bisect the v1.491 1716ms-frame issue at MP playtest time. After the bisection identifies which subsystem is the dominant Tier S contributor, the actual reclaim ship (coalesce/throttle the broadcast) follows. v1.491 (2026-05-08) — **NEW FAILURE MODE: per-frame CPU cap, not heap.** v1.491 8–10 player MP playtest hit `Mod has been running for 1,716ms this frame which exceeds max evaluation time of 1,000ms`. The script terminated on a single-frame spike, **not** on cumulative heap. This is a different regime from the v1.406 16-player heap-OOM that drove waves 1–6 — that was about retained per-player allocations; this one is about synchronous work piled into one frame. **A new "Per-frame CPU spikes — Tier S" framework is appended below the heap-retention ladder.** Suspect S1: `updateConquestCombatHudForAllPlayers(force=true)` from `OnPlayerEnter/ExitCapturePoint` ([`area-triggers.ts:47,66`](../src/index/area-triggers.ts#L47-L66)) bypasses the 200ms HUD throttle at [`pipeline.ts:118-121`](../src/ui/conquest/hud-core/pipeline.ts#L118-L121); a cluster of 5 players entering a flag fires 5 unthrottled N-player full-VM-build passes back-to-back. Full evidence + ranked S1–S10 suspect list in [`conquest_optimization_state.md`](./conquest_optimization_state.md) "Per-frame CPU fan-out — v1.491 crash hypothesis" section. This supersedes (does not replace) the heap reclaim ladder — both budgets are real, and the next reclaim work should target Tier S items first since they're the active blocker. v1.474 fixed CQ_Bug_115 (late-joiner during pregame countdown cancels the countdown via per-frame `areAllActivePlayersReady` check, locking all watchers out of deploying). Single-line removal in [`src/ready-dialog/countdown-flow.ts:61`](../src/ready-dialog/countdown-flow.ts#L61). MP-confirmed at 2 players (2026-05-08). Bundle 865,714 / 1,048,576 (−57 bytes from v1.473; 17.44% headroom). Plan: [`5.07.26_late_join_during_countdown_fix_plan.md`](./5.07.26_late_join_during_countdown_fix_plan.md). v1.473 added 2 yellow-smoke supply boxes (1058+1059) on Operation Firestorm via single-file config edit (no PPM impact; +294 bytes). v1.471 shipped late-join crash defense bundle (CQ_Bug_114): Phase A outer try/catch on `OnPlayerJoinGame` async export, Phase B pre-await `vehicleDeployTimerCache[pid]` delete to close race against `runRoundStartDelayHudLoop`, Phase C post-await idempotent re-read of `perspectiveTeamByPid` for late-joiner team-bind lag. +420 bytes; MP repro validation pending. Plan: [`5.04.26_late_join_crash_defense_plan.md`](./5.04.26_late_join_crash_defense_plan.md). v1.470 shipped C3 audit on `ui/conquest/hud-core/constants.ts` (Option B path: Wave 6 Ship 1c rubble cleanup). Dropped 9 dead consts (2 team-name-shadow stragglers from the v1.449/v1.451 backplate churn + 7 SHADOW_OFFSET passthroughs to a function that ignored its arg) + simplified `twlConquestHudBuildShadowRingProfile` signature from `(offset, upScale, includeInner)` to `()` since all 3 params were dead since v1.443. 8 call sites updated to no-args form. Bundle 865,057 / 1,048,576 (−1,311 bytes from v1.469; Wave 6 Ship 1c rubble accumulated 56 versions before being noticed). Plan archive: [`5.05.26_conquest_hud_core_constants_audit_plan.md`](./5.05.26_conquest_hud_core_constants_audit_plan.md). Audit findings: out of 162 consts, 23 had zero external readers; 14 of those were Bucket C-live (intra-file chain reaches an external consumer — KEEP); 9 were truly dead (delete). No `foundation/ui-layout.ts` audit yet — the 7 deleted SHADOW_OFFSET consts each pulled their value from a corresponding `CONQUEST_HUD_*_SHADOW_OFFSET` const there, which may now also be dead (separate audit candidate). v1.469 shipped Tier A1 cleanup (the cheapest item on the entire ladder, dormant since v1.406): dropped 11 read-zero fields from `VehicleSpawnerSlot` + 2 orphan type aliases + a stale watchdog comment. Bundle 866,368 / 1,048,576 (−920 bytes from v1.468; better than the estimated −700 — the type aliases came along for free). Heap: 11 fields × 16 vehicle slots = ~176 fewer property allocations every match. No PPM changes (slot is slot-keyed not pid-keyed). Zero risk: every dropped field had read count = 0 across `src/`. v1.468 trivial fix: 1-line `STR_UI_WAIT` alias declaration in `foundation/string-keys.ts:100` to resolve 6 standalone-tsc errors that v1.467 emitted (the bundle still shipped because `@ts-nocheck` skips the file's symbol checks at bundler time, but the postbuild `cmd /c npx tsc --pretty false --noEmit` step caught the missing alias). Bundle 867,288 / 1,048,576 (177 KB headroom; +48 bytes from v1.467). No PPM impact. Worth flagging for future: a `@ts-nocheck` file's missing-symbol references will silently emit through the bundler — only the standalone tsc postcheck catches them. v1.467 supply box: per-class tiles render yellow "WAIT" instead of green "READY" while `roundStartGadgetDelay` gate is active (Firestorm: first 120s of LIVE), then flip back to green READY once the gate elapses. In-class only (off-class red preserved); cooldown timers preserved (only the would-be-READY label flips). All 6 per-tile sigs gained `gadgetBlocked ? 1 : 0` to guarantee refresh on transition (covers edge cases like engineer-with-zero-ammo-charges where the existing `enabled` flag wouldn't flip with the gate). No new strings (`twl.ui.wait` reused from v1.446 vehicle deploy). Top-of-menu countdown ("Gadgets at this Supply Box will be available in {0}s...") unchanged. Bundle 867,240 / 1,048,576 (+1,209 bytes from v1.466). v1.466 added per-player engine-deploy block during HQ-claim window via centralized helper `setVehicleDeployEngineDeployBlockForPid(pid, blocked)` wrapping `mod.EnablePlayerDeploy(player, !blocked)`. Resolves console seat race (Bug #113); per-player only, never touches global `mod.EnableAllPlayerDeploy`. Bundle 866,031 / 1,048,576 (177 KB headroom; +869 bytes from v1.465). v1.465 moved Vehicle Deploy click action to fire on first primary-click event via `tryConsumeUIButtonPrimaryClickEvent` dedupe (mirrors team-swap pattern); +842 bytes; resolves console click recognition. v1.464 removed inert Blur+Fill widgets from vehicle deploy button chrome (M1 reclaim ~18 widgets/pid; ~288 widget refs at 16p) + 7 stale Checkbox* dead-cleanup hooks; −4,920 bytes. v1.463 removed text-shadow widgets from vehicle deploy button + label chrome (M1 reclaim ~17 widgets/pid; ~272 widget refs at 16p; visible visual change — text loses drop-shadow). **Cumulative v1.463 + v1.464 M1 reclaim: ~35 widgets/pid (~560 widget refs at 16p).** M1 (vehicleDeployTimerCache) re-measure: ~310 → ~275 widgets/pid post-v1.464. Still the largest single per-pid heap allocator. v1.459 stripped Host concept + first-joiner auto-admin reclaim. `admin/identity.ts` shrank ~110 → 64 lines (host state + isHost + getHostFirstPid + getHostNameMessage + auto-promote branch deleted); Player Ready Panel dropped Game Host row (~19 widgets/pid, was ~20); `gameHostFormat` string + widget id deleted; `Admin.onPlayerJoin` call site removed from `player-join-leave.ts`. Bundle 872893 / 1048576 (171 KB headroom; net -1,821 bytes from v1.457). Host was never script-authoritative; admin slot now claim-only via existing CLAIM ADMIN flow. No PPM impact. v1.457 fixed delay-broadcast text-widget visibility (text was created visible:false; broadcasts only toggled root) + bumped Y 150→220 to clear engage panel. v1.455 added Delay-Elapsed Broadcasts feature: new file `src/index/delay-broadcast.ts` (~145 lines) + new `M16` PPM entry for `delayBroadcastByPid` (smallest M-tier; 2 widgets/pid lazy-built post-LIVE; ~32 widget refs at 16 players). Token-based cancel pattern, wired into `startMatch`/`endMatch`/`triggerFreshMatchSetup` + `cleanupHudForPid`. Independent surface (NOT child of engage-root); pixel-positioned at TopCenter Y=150. Late-joiners do not see past broadcasts (per Q5 user direction). Bundle 874586/1048576 (169 KB headroom). v1.454 tightened vehicle deploy timer row stride from 32 to 23 (`ROW_HEIGHT` 30→22, `ROW_GAP_Y` 2→1) per UI tuning pass. v1.453 finalized engage-status backplate dimensions (98×14, Y+2). All pure layout/visual changes; no PPM impact. v1.451 dropped team-name backplates entirely (looked weird in top-HUD spacing per SP feedback). Net +1 widget/pid in M3 vs pre-v1.449 (~92 → ~93) — only `engageStatusBox` remains. v1.449 originally shipped 3 backplates (team names + engage status) at +3 widgets/pid; v1.450 was first dimension polish; v1.451 reduced scope to engage-only. Reuses tickets-box visual style (Blur fill, dark color, 0.75 alpha). Recovers legibility lost when Wave 6 Ship 1c eliminated the 8-layer compass shadow rings. v1.447 + v1.448 shipped CQ_Bug_94 in two passes: v1.447 fixed the menu-OPEN path (per-class HasEquipment probes for Assault/Medic/Recon; +2,614 bundle bytes), v1.448 followed up to fix the menu-PLACEMENT path (dropped `isSlotEmpty` precheck from non-Engineer give helpers; was the remaining `GetInventoryAmmo` source; −362 bundle bytes). Combined: non-Engineer classes emit ZERO `GetInventoryAmmo` engine error log entries on either menu-open OR placement; Engineer untouched throughout. Heap-pressure contributor against #109 budget eliminated; no PPM impact. v1.445 shipped CQ_Bug_58 (ready-state auto-unready triggers locked at 2; deleted 3 lines + 1 function — no PPM impact, slight bundle shrink). v1.446 shipped CQ_Tweak_WAIT_Label (added 1 widget per timer row to M1 — see M1 row update below). Wave 6 Ship 0+1c+1d shipped at v1.443+v1.444 (combat HUD compass shadow rings eliminated, ~280 widgets/pid reclaim, ~75% of M3 cache). M1 + M3 rows + ladder Tier A entries updated below to reflect current state. Originally re-issued at v1.406 under a **runtime JS heap** lens after the 16-player MP playtest crashed at script load with `Mod has reached its js script memory usage limit. It has been terminated` (issue [#109](./conquest_issues.md#cq_bug_16player_playtest_js_memory_limit-109)). The reclaim ladder below ranks levers by **expected runtime memory reduction**, not by emitted bundle bytes.

Companion docs:
- [`conquest_optimization_state.md`](./conquest_optimization_state.md) — sister log file. File map (lines, bytes, per-player multipliers, in-bundle status) + per-file function inventory. The *facts*; this doc holds the *reasoning*.
- [`conquest_design.md`](./conquest_design.md) — locked design rules (architecture, UI contract, CF gameplay rules, vehicle patterns) + new-design space.
- [`conquest_issues.md`](./conquest_issues.md) — full issue bodies.
- [`conquest_issues_summary.md`](./conquest_issues_summary.md) — issue index.

---

## TL;DR (v1.495 measurements; v1.406 baseline noted inline for delta)

> **Re-measured v1.497, 2026-05-09.** Numbers below reflect the current state per direct measurement of `dist/bundle.ts`. v1.406 baseline values are bracketed for delta comparison. The qualitative conclusions are unchanged — bundle is still well under the 1 MiB cap, but the binding constraint is now **per-frame CPU cap** (Tier S, post-v1.491) rather than heap (Tiers A-F, post-v1.406).

1. **Failure mode shifted three times.** (a) v1.406: 16-player heap-OOM drove waves 1–6 of heap reclaim (Tiers A–F). (b) v1.491: 8–10 player per-frame CPU spike (1,716ms / 1,000ms cap) drove the Tier S framework. (c) Within-match accumulation (user-clarified 2026-05-09): the script restarts at match end and the v1.491 breach manifests within a single match (20+ min), so multi-match drift is NOT the issue and game-mode restart cadence is NOT a valid mitigation. Bundle is **925,574 bytes** at v1.497 *(was 872,014 at v1.406; +53,560 net across the entire arc — Waves 1-6 reclaimed ~6,300 bytes which was outweighed by v1.492's +42,839 bytes when `FEATURE_ADMIN_PANEL` + `FEATURE_POSITION_DEBUG` were re-enabled to support the admin diagnostic toggles; v1.495–v1.497 added +464 bytes net for the long-match audit + Tier S3 ship)*, with **11.73% headroom** under the cap. The 1 MiB upload cap is no longer the binding constraint. The current binding constraint is the **per-frame eval budget** (1,000ms hard cap, opaque). Heap remains a secondary concern but appears stable post-Wave-6. **Tier S3 SHIPPED v1.497** (200ms-coalesce throttle on `updateVehicleDeployTimerHudForAllPlayers`); MP playtest with diagnostic toggles is the gate to confirm impact. S1/S2 user-approved and pending. Tier A work resumes after MP stability is restored.

   **v1.494 follow-up audit (2026-05-09): within-match accumulation regime.** User reported that the v1.491 spike at 8–10 players is associated with **time-in-match (20+ minutes within a single match)** rather than per-event burst density — capture-point storms during explicit testing did NOT directly trigger spikes. The script restarts at match end, so this is not a multi-match drift problem; the accumulation happens entirely *within* one match. Static audit of `src/` for monotonic-growth records (full report in [`conquest_optimization_state.md`](./conquest_optimization_state.md) "Long-match accumulation audit") found 5 confirmed script-side leaks (L1-L5), all small. **L1 (`vehicleSpawnBaseTeamByObjId`) — DEAD-CODE-DELETED v1.496** (post-A11 grep confirmed zero readers; the cache was write-only and reclassified Tier C; A11's leak-fix framing was wrong since GC would have collected entries at game-mode init anyway). **L2-L5 are tiny** (per-pid scalar trackers without leave-time delete). **None individually explain a 1,716ms CPU spike via heap pressure.** Implication: the v1.491 within-match driver is most likely engine-side state we cannot statically audit (VFX retention, world-log buffer, area-trigger churn, capture-sound queue) OR a Tier S burst event whose trigger conditions become more likely as in-match state accumulates. Remaining Tier A entries: **A12 (L3-L5 click-tracker fixes, pending), A13 (L2 lockerSlotToggle, user-approved 2026-05-09 pending implementation)**.
2. **No engine telemetry exists for the heap budget.** Verified against [`reference_sdk_1.2.3/code/types/mod/index.d.ts`](../../reference_sdk_1.2.3/code/types/mod/index.d.ts): no `mod.GetScriptMemory*`, `mod.GetHeapSize`, or comparable surface. The budget is observed only at termination. Reclaim has to be measured by **inference + cumulative reduction**, not by direct readings.
3. **Per-player multipliers dominate.** Module-level constants are paid once; per-player widget caches, view-model objects, and PID-keyed records pay N times. Fifteen of the largest known allocators scale with player count. The ladder targets those first.
4. **Functionality is locked.** The user's directive: no UI look change, no feature behavior change. Reclaim is restricted to (a) dead-code removal, (b) inlining of variables that exist purely for naming with no runtime dynamism, (c) helper extraction that *reduces* identifier count, (d) cache/state thinning where the field is write-only or holds a redundant duplicate.
5. **All design changes require user approval.** This document inventories candidates and ranks them. Any item the user does not pre-authorize stays in "proposed" status until they sign off.

**Playtest-blocking items:** [#109](./conquest_issues.md#cq_bug_16player_playtest_js_memory_limit-109).

---

## Why memory, not bytes (the regime change)

The previous baseline of this document was written when `dist/bundle.ts` sat 13–25 KB below the 1 MiB upload cap and every feature add risked failing the upload check in `scripts/verify.js`. v1.397's whitespace strip and v1.398's block-comment strip moved that cliff far enough away that the upload cap is no longer where the project hits a wall.

The 16-player playtest (v1.406) crashed differently: not at upload, but at script execution. The Mod Evaluator terminated the script while running with the message about JS memory limits. That distinguishes two budgets:

- **Upload byte cap** — `1,048,576` bytes against `dist/bundle.ts`. Enforced at submit time. Linear in bundle bytes, not in player count.
- **Runtime heap cap** — opaque, enforced at execution. Grows with both retained module-level state AND per-player allocations. Crosses the threshold at the player-count where the per-player factor compounds enough.

Bundle bytes correlate weakly with heap. A single 200-byte `Record<number, T>` declaration creates ~zero heap before any pids exist; once 16 players join and each one materializes the lazy entry, that record holds 16 pid keys × N fields × heap-aligned object overhead. The byte-count pass missed this entirely.

This is why reclaim that previously felt minor (-700 bytes) is suddenly load-bearing if it's *per-player*: removing 11 dead `VehicleSpawnerSlot` fields × 16 slots is 176 small allocations the engine doesn't have to track.

---

## Inventory of per-player multipliers

The following structures all scale with connected player count. Each row is a candidate target for the reclaim ladder.

**Ranking convention.** `M1` is the **worst** allocator — largest expected heap retention at 16 players. `M15` is the **least** impactful. The numeric ID *is* the rank. The `Scale` column is the comparable bucket so two allocators with the same scale are roughly equal in impact.

**Scale buckets** (per allocator, totaled across 16 players):

| Bucket | Approx. size | Meaning |
|--------|--------------|---------|
| **XL** | >1,500 widget refs / objects retained | Dominant heap contributor at 16p |
| **L** | 200–1,500 retained | Heavy multiplier; first-tier reclaim target |
| **M** | 50–200 retained | Material at scale; plausible reclaim target |
| **S** | 10–50 retained | Small but still per-pid |
| **XS** | <10 retained, OR feature-flagged off currently | Negligible at present player counts |
| **Churn** | Per-tick allocations, GC pressure | Not retained but produces heap churn that the runtime tracks |
| **Variable** | Hard to estimate statically | Closure capture, depends on scope |

The IDs are referenced from the `PPM` column of the file map in [`conquest_optimization_state.md`](./conquest_optimization_state.md) — change one, update both.

| Rank ID | Scale | Allocator | Source | Per-player heap (est.) | Total at 16 players | Notes |
|:-------:|:-----:|-----------|--------|-----------------------|---------------------|-------|
| **M1** | **XL** | `State.hudCache.vehicleDeployTimerCache[pid]` | [state/hud-cache-types.ts:104](../src/state/hud-cache-types.ts#L104) | **Re-measured post-v1.464: ~275 widget refs/pid** — down from ~310 pre-v1.463. v1.463 removed 5 text-shadow widgets per row + 1 close-button-text-shadow (+~17/pid reclaim; user-accepted visual change to no-drop-shadow text). v1.464 removed 4 Blur+Fill widgets per row (`spawnButton{Blur,Fill}`, `groundButton{Blur,Fill}`) + 2 panel-level (`closeButton{Blur,Fill}`) (+~18/pid reclaim; zero visual change — these were always-hidden scaffolding). **Cumulative v1.463+v1.464 reclaim: ~35 widgets/pid.** Current shape: ~9 root widgets (root, livePanelBlur×3, closeButton×3) + 12 rows × ~22 widgets each (~14 main row widgets + 7 in nested `ReusableTimerWidgetCacheEntry` post-Wave-5 + v1.446 `barText`). Doc previously assumed "~6-10 typical rows" but `VEHICLE_DEPLOY_TIMER_MAX_ROWS = 12` (foundation/ui-layout.ts) — slot count is a fixed cap. | **~4,400 widget refs** at 16p *(was ~4,960 at v1.454; v1.463+v1.464 cumulative reclaim: ~560 refs at 16p)* | `VehicleDeployTimerRowCacheEntry` per row carries `lastShowSpawnButton`, `lastShowGroundButton`, `lastSpawnButtonVisualState`, etc. — diff-cache fields that are flag-state mirrors. v1.446 added `barText` widget on every reusable timer instance via `buildReusableTimerBarText` (+1 widget/row × 12 rows × 16p = +192 refs). v1.463 + v1.464 are the first ships explicitly targeting M1 since Wave 6 — both shipped 2026-05-04 in same-day cleanup pass. M1 remains the largest single per-pid heap allocator post-reclaim. Plans: [`5.04.26_conquest_vehicle_deploy_shadow_removal_plan.md`](./5.04.26_conquest_vehicle_deploy_shadow_removal_plan.md), [`5.04.26_conquest_vehicle_deploy_inert_widget_cleanup_plan.md`](./5.04.26_conquest_vehicle_deploy_inert_widget_cleanup_plan.md). |
| **M2** | **XL** | `State.hudCache.ammoResupplyMenuCache[pid]` | [state/hud-cache-types.ts:188](../src/state/hud-cache-types.ts#L188) | **Re-measured v1.454: ~250 widget refs/pid when fully built** — 23 root widgets (borders, title, class headers, close button, gadget delay, help text) + ~154 charge-entry widgets (assault/grenades/recon/medic/engineer charge clusters × 11 widgets each) + 4 classes × 12 action-row widgets + ~8 header array `h` + 4 classes × 5 slot-toggle widgets = ~253 total. **Lazy-built**: only allocated on first menu open, then retained until disconnect. Doc's prior 100-180 range reflected partial-roster-built state. | **~4,000 refs** at 16p (full-built); **less if some pids never open the crate** (Ship 3.5 LIVE-phase warm stagger gradually builds opt-in) | Wave 3 Ship 3 (v1.411): built first-interact-only via `triggerLazyBuild('supplyBox', pid)` in `openArmMenu`. Ship 3.5 (v1.412) adds an opportunistic 2s/pid LIVE-phase warm stagger; pids that never visit the crate never build. v1.454 re-measure shows when built the cache is larger than originally claimed (~250 vs 100-180). |
| **M3** | **L** | Combat HUD entry graph (`twlConquestHudEnsurePlayerGraph`) | [ui/conquest/hud-core/build.ts:198](../src/ui/conquest/hud-core/build.ts#L198) | **Post-v1.451:** ~93 widgets/pid (Wave 6 Ship 1c brought it to ~92; v1.449 added +3 backplates; v1.451 removed 2 of those — net +1 vs pre-v1.449). Tickets (4) + tickets bg (2) + team labels (2) + objective slots (7 × 5 base widgets, no shadow rings) + chevrons (14 base, no shadow rings) + popout panel (~6) + engage panel (~7) + **engage-status backplate (1 — NEW v1.449, retained v1.451)** + crown shadows (2 image widgets, NOT in ring system) + containers/frames (~16) + 7 view-model snapshots. | **~1,488 refs** at 16p (was ~1,472 pre-v1.449; was ~5,952 pre-Wave-6). | Wave 6 Ship 1c (v1.443) eliminated the 8-layer compass shadow rings via empty-profile in `twlConquestHudBuildShadowRingProfile`. Crown image shadows (2 widgets/pid) preserved; chevron icon widgets preserved. Wave 6 chevron-color polish (v1.444) inverts left/right chevron colors for contrast post-shadow-removal. **v1.449 (HUD backplates)**: +3 widgets/pid (`ticketBlueTeamNameBox`, `ticketRedTeamNameBox`, `engageStatusBox`). **v1.450**: dimension polish (team-name height matches tickets; engage box switched to dedicated tight constants). **v1.451**: team-name backplates removed entirely (looked weird in top HUD spacing per SP feedback) — only `engageStatusBox` remains. Engage box width tightened 110→90. **Re-ranking note:** post-v1.451 M3 (~1,488) still slots between M2 (~1,600-2,900, latent) and M4 (~912). Once M2 settles to its lazy-built steady state, M3 may genuinely be the smallest L-tier allocator. Re-confirm with MP playtest signal. |
| **M4** | **L** | `State.hudCache.topHudShellByPid[pid]` ([TopHudShellRefs](../src/state/hud-cache-types.ts#L3)) | [state/hud-cache-types.ts:3](../src/state/hud-cache-types.ts#L3) | ~25 named widget refs + `victoryLeftRosterText`/`victoryRightRosterText` arrays (sized to roster len) + `roots` array | ~400 base + 16 × 16 × 2 = **~912 refs at full lobby** | The two roster arrays alone are 512 widget refs at full lobby |
| **M5** | **M** | `State.hudCache.clockWidgetCache[pid]` + `countdownWidgetCache[pid]` | [foundation/ui-layout.ts](../src/foundation/ui-layout.ts) drives schema | **Re-measured v1.454: ~38 widget refs/pid total** — clock cache is `ReusableTimerWidgetCacheEntry`-shaped (~14 widget refs + 2 diff fields), countdown cache is smaller (~5 widgets including delay-line array). | **~608 refs** at 16p *(v1.406 doc claimed ~480; re-measure shows previous estimate was slightly low by ~128 refs)* | Two parallel caches per pid for the same MM:SS shape — consolidation candidate. The v1.439 Wave 5 timer simplification reduced widget count per cache (10 digits/shadows replaced with 2 bar widgets); v1.446 added `barText` per timer instance. |
| **M6** | **M** | `State.hudCache.boundaryPromptCache[pid]` | [state/hud-cache-types.ts:127](../src/state/hud-cache-types.ts#L127) | 12 widget refs + 12 matched name strings + 3 `last*` diff-cache fields | **~432 entries** (~27 × 16) | Each entry stores both `rootName`/`panelName`/`*Name` strings AND the resolved `mod.UIWidget` ref — duplicate state |
| **M7** | **M** | 11 PID-keyed records in `State.players.*` (`deployedByPid`, `deployedAtSecondsByPid`, `disconnectedByPid`, `uiInputEnabledByPid`, `liveVehicleDeployMenuVisibleByPid`, `posDebugTransformSourceByPid`, `posDebugVehicleObjIdByPid`, `inMainBaseByPid`, `readyByPid`, `readyNeedsReconfirmByPid`, `readyMessageCooldownByPid`) | [state/runtime-types.ts:380-465](../src/state/runtime-types.ts#L380) | 11 PID-keyed records | **~176 entries** (11 × 16) | Was 12 pre-v1.418; `warmPrimeActiveByPid` deleted in Wave 3 Ship 8. **v1.492:** `FEATURE_POSITION_DEBUG` re-enabled. `posDebug*` records are now WRITTEN — but only for the admin pid (manual-toggle only; `autoStartPositionDebugOnDeploy` was no-op'd in v1.492). Effective per-pid cost remains zero for all but 1 admin slot. |
| **M8** | **M** | `State.players.worldInteractableIconByPidByObjId[pid]` | [state/runtime-types.ts:387](../src/state/runtime-types.ts#L387) | Per-pid `Record<number, any>` of WorldIcon refs (one per active interactable) | **~128 icon refs** (~8 × 16) | Keep — load-bearing per-player visibility |
| **M9** | **S** | 7 PID-keyed records in `State.conquest.debug.*` (`hudGenerationByPid`, `combatHudGenerationByPid`, `teamSwapRefreshTokenByPid`, `teamSwapHudResetPendingByPid`, `perspectiveTeamByPid`, `teamSwapPerspectiveLockUntilByPid`, `engageHiddenUntilDeployByPid`) | [state/runtime-types.ts:270-276](../src/state/runtime-types.ts#L270) | 7 PID-keyed records, mostly numeric scalars | **~112 entries** (7 × 16) | Several may be consolidatable into one struct per pid (`hudPerPid[pid]`) |
| **M10** | **S** | `State.players.armS[pid]` / `armG[pid]` / `armL[pid]` / `armO[pid]` / `armI[pid]` / `armT[pid]` / `armFocusedTileKeyByPid[pid]` | [state/runtime-types.ts:388-393](../src/state/runtime-types.ts#L388) (corrected v1.454 audit — was `:387` which is M8's line) | 7 small per-pid structs for the resupply menu | **~112 small allocs** (7 × 16) | Fragmented; consolidating to one `armState[pid]` reduces alloc count by 6× |
| **M11** | **Churn** | `State.conquest.debug.hud{Status,HelpReady,Clock}VmByPid[pid]` | [state/runtime-types.ts:277-291](../src/state/runtime-types.ts#L277) | Three view-model objects per pid; rebuilt every dirty tick | **~48 short-lived allocs per dirty render** (3 × 16) | Each tick spawns new VM objects without reusing the prior ones — heap churn driver, not retention |
| **M12** | **S** | `State.players.kpiByPid[pid]` | [state/runtime-types.ts:470](../src/state/runtime-types.ts#L470) | 7-field score object | **~112 numeric fields** (7 × 16) | Live state — keep |
| **M13** | **XS** | `State.players.uiCachePerfByPid[pid]` | [state/runtime-types.ts:440](../src/state/runtime-types.ts#L440) | 3 sub-objects (`vehicle`/`ready`/`gadget`) × 4 numeric counters each | **0 currently** (only populated when `FEATURE_PERF_DIAG = true`); ~192 fields when on | Currently dead at runtime — type+init still live; Tier A5 strips |
| **M14** | **XS** | `State.players.lockerSlots[pid]` + `lockerSlotToggle[pid]` | [state/runtime-types.ts:414](../src/state/runtime-types.ts#L414) | `{ g1, g2, initializedAt }` + `{ slotByClass: [1,1,1,1] }` | **~32 entries** (2 × 16) | Drops on menu close per design (correct) — keep |
| **M15** | **Variable** | Closures retained by `Timers.setTimeout` / `mod.Wait` continuations | scattered across `actions.ts`, `vanilla-spawner.ts`, `hq-deploy.ts`, `ammo-resupply-menu.ts`, `deploy-timer-ui.ts` | Each pending continuation captures its enclosing function scope; material at scale | Variable; **a deploy-timer 1 Hz loop per visible viewer × 16 players × N slots is tens of pending continuations** | Hard to inventory statically; needs per-site audit for closure-captured locals that could be passed as arguments instead |
| **M16** | **XS** | `State.hudCache.delayBroadcastByPid[pid]` | [state/hud-cache-types.ts:233-237](../src/state/hud-cache-types.ts#L233) | **v1.455 (Delay Broadcasts)** — 1 cache entry per pid: `{ root, text, hideTimerId }`. Lazy-built on first broadcast post-LIVE; hidden ~99% of round. Up to 4 ephemeral `Timers.setTimeout` handles globally + 1 hide-timer handle per pid. | **~32 widget refs at 16 players** (2 × 16, after first broadcast fires) | Smallest M-tier entry; delete-on-leave reaches `cleanupHudForPid` via `destroyDelayBroadcastWidgetForPid`. No per-tick churn. |

**Reading the table:** if the 16-player crash is dominated by retained heap, focus reclaim on **M1–M4** first (XL+L). If it's dominated by churn or GC pressure, **M11** matters more than its position suggests. If it's dominated by closure pinning, **M15** is the wildcard. **M16** (delay broadcasts) is the smallest entry — flagged for completeness, not as a reclaim target. The 16-player playtest can't distinguish these without telemetry the SDK doesn't expose, so the ladder ships top-down.

---

## Inventory of one-time (non-per-player) overhead

These are paid once but are still resident heap that the runtime tracks.

> **Re-measured v1.468, 2026-05-05.** O1/O2 counts directly grep'd from `dist/bundle.ts` (`^const ` / `^let ` patterns). O3 byte count from `wc -c dist/bundle.strings.json`. Methodology note for O1: the v1.406 measurement (3,231) used an unspecified tool; the current grep counts only `^const ` line starts and may differ slightly from a full AST scan. Both numbers represent module-level declarations.

| # | Source | Quantity (v1.468) | v1.406 baseline | Delta | Reclaim feasibility |
|---|--------|-------------------|-----------------|-------|---------------------|
| O1 | Module-level `const` declarations in bundle | **3,332** | 3,231 | +101 (+3.1%) | High — many are layout constants whose value is set once and never reassigned. They could be *inlined* at use sites, removing the binding from the heap. |
| O2 | Module-level `let` declarations in bundle | **190** | 171 | +19 (+11.1%) | Mixed — some are real mutable singletons (`ACTIVE_GADGET_CONFIG`, `suppressReadyDialogModeAutoSwitch`); others may be inlinable. Growth driven by Wave 3 lazy-build registry + Wave 4 admin slot state + per-tick caches. |
| O3 | `bundle.strings.json` keys | **23,152 bytes** | 22,082 | +1,070 (+4.8%) | Medium — ~8.8 KB are dead (Cat 8 below). Growth since v1.454 driven by v1.455 delay-broadcast strings (4 keys), v1.461 admin-hint strings (2 keys), v1.467 supply-box reuses existing `twl.ui.wait` (no new keys). Strings live separately from script heap but still occupy runtime memory. |
| O4 | `enum`s (`TeamID`, `MatchPhase`, lifecycle phases) | 5–10 enum tables | Low — enums are small and structurally needed |
| O5 | Compile-time-stripped feature code (`FEATURE_PERF_DIAG = false`, `FEATURE_JOIN_PROMPT = false`; **`FEATURE_ADMIN_PANEL = true`** as of v1.492; **`FEATURE_POSITION_DEBUG = true`** as of v1.492) | Two of four flags now stripped (was four). The two re-enabled flags add ~46K bundle bytes back. Admin-pid scope keeps heap impact at ~80 widget refs (admin only) — does not introduce a new M-tier entry. | N/A — admin diagnostic toggles need this code in the bundle. |

---

## The reclaim ladder (ranked by player-count multiplier)

Each tier estimates **runtime heap reduction** and **expected effort**. The user must approve each before any code change. These are documentation entries until then.

### Tier A — Per-player widget cache thinning (HIGHEST ROI per player)

| # | Lever | Target file(s) | Mechanism | Heap impact (16p) | Bundle impact | Risk | Approval status |
|---|-------|----------------|-----------|-------------------|---------------|------|-----------------|
| **A1** | **Remove 11 write-only `VehicleSpawnerSlot` fields (Cat 7 carry-over; not a per-pid `Mn` — slot-keyed not pid-keyed)** | [state/runtime-types.ts:6-42](../src/state/runtime-types.ts#L6), [vehicles/vanilla-spawner.ts:223-247](../src/vehicles/vanilla-spawner.ts#L223) | Dropped fields with reads = 0 across `src/`: `enableToken`, `spawnRequestToken`, `spawnRequestAtSeconds`, `expectingSpawnStartedAtSeconds`, `respawnQueuedAtSeconds`, `respawnReadyAtSeconds`, `lastSpawnedAtSeconds`, `lastDestroyedAtSeconds`, `lastMissingAtSeconds`, `spawnCategory`, `availabilityPhase`. Also removed 2 orphan type aliases (`VehicleSlotSpawnCategory`, `VehicleSlotAvailabilityPhase`) and a stale `pollVehicleSpawnerSlots` watchdog comment. Kept `vehicleId`, `*Pos`/`*Rot`, `pendingSpawnMode`, `respawnClock`, `expectingSpawn`, `respawnRunning`, `deployFlowTracked` (verified read). | 11 fields × 16 vehicle slots = **176 fewer field allocations** | **−920 bytes** (better than estimated −700, the orphan type aliases helped) | **Zero** — reads = 0 verified via grep | **SHIPPED v1.469** |
| A2 | Drop diff-cache `last*` mirror fields where the source is already authoritative (targets **M1**, **M5**, **M6**) | `VehicleDeployTimerRowCacheEntry` (`lastShowPlayerName`, `lastShowSpawnButton`, `lastShowGroundButton`, `lastSpawnButtonVisualState`, `lastGroundButtonVisualState`, `lastVisibleState`, `lastPlayerNameVisible`, `lastSpawnButtonVisible`, `lastGroundButtonVisible`); `BoundaryPromptWidgetCacheEntry` (`lastVisibleState`, `lastKind`, `lastRemainingSeconds`); `VehicleDeployTimerHudCacheEntry` (`lastVisibleState`, `lastRenderSignature`, `lastCloseButtonVisualState`, `lastLiveTerminalChromeVisible`, `lastCloseButtonVisible`) | Replace each `if (cache.lastX !== newX)` check with one of: (a) shared `lastRenderSignature` string check, (b) write-through where engine accepts redundant writes cheaply, (c) read-back from widget if API allows | Hard to estimate — at minimum **~10–15 numeric/string fields × N rows × 16 pids**, possibly hundreds | −300–600 bytes | **Medium** — diff caches were added to suppress redundant `mod.SetUI*` calls; some are perf-load-bearing. Each removal needs profiling justification | **Needs user discussion** — the user explicitly mentioned UI variables existing without runtime dynamism. These are the most likely targets but each removal needs to be benchmarked for engine-call overhead it re-introduces |
| A3 | Consolidate `armG`/`armL`/`armS`/`armO`/`armI`/`armT`/`armFocusedTileKeyByPid` into one `armState[pid]` (targets **M10**) | [state/runtime-state.ts:213-220](../src/state/runtime-state.ts#L213), [interaction/ammo-resupply-menu.ts:187-258](../src/interaction/ammo-resupply-menu.ts#L187) | One per-pid object instead of seven; same fields under one parent | Saves 6 × 16 = **96 small allocations** + 6 record headers. Same field count overall but one parent allocation per pid instead of 7 | Slight increase (+200 bytes for the type + accessor refactor) | Low — mechanical refactor | Pending |
| A4 | Strip `posDebugTransformSourceByPid` + `posDebugVehicleObjIdByPid` from production bundle (subset of **M7**) | [state/runtime-types.ts:468-469](../src/state/runtime-types.ts#L468) | Both unused when `FEATURE_POSITION_DEBUG = false` (current). Type and init still live | 2 records × 16 = **32 entries** at lobby fill | −small | Zero — gated feature is off, has been for 80+ versions | Pending |
| A5 | Drop `uiCachePerfByPid` from production scope when `FEATURE_PERF_DIAG = false` (targets **M13**) | [state/runtime-types.ts:440-459](../src/state/runtime-types.ts#L440), [state/runtime-state.ts:223](../src/state/runtime-state.ts#L223) | Same shape: type + init resident but never written | 1 record × 12 fields × 16 = **192 fields** when populated | −small | Zero — the `mod.AllPlayers` perf counter increment sites are already feature-flag-gated and stripped | Pending |
| **A6** | **Plug `ammoResupplyMenuCache[pid]` leak on player disconnect (Lifecycle Map finding — targets M2)** | [index/player-join-leave.ts:138](../src/index/player-join-leave.ts#L138) | `destroyArmMenu(pid)` added after `resetArmState(pid)` in `onPlayerLeaveGameImpl`. | Per leak: 100–180 widget refs. Cumulative: was unbounded. | ~0 (one extra function call) | **Zero** | **Resolved (v1.407, pending MP confirm — see [`conquest_mp_ongoing_tests.md`](./conquest_mp_ongoing_tests.md) Wave 1)** |
| **A7** | **Plug `hqDeploy.lastRequestAtSecondsByPid` leak on player disconnect (Lifecycle Map finding)** | [state/runtime-types.ts:494](../src/state/runtime-types.ts#L494), [index/player-join-leave.ts:158](../src/index/player-join-leave.ts#L158) | `delete State.hqDeploy.lastRequestAtSecondsByPid[pid];` added in `onPlayerLeaveGameImpl`'s per-pid delete cluster. | Per leak: 1 number per pid. Cumulative: was unbounded. | ~0 | **Zero** | **Resolved (v1.407, pending MP confirm — see [`conquest_mp_ongoing_tests.md`](./conquest_mp_ongoing_tests.md) Wave 1)** |
| **A8** | **Eliminate combat HUD 8-layer compass shadow rings (targets M3) — Wave 6 Ship 1c** | [ui/conquest/hud-core/constants.ts:256](../src/ui/conquest/hud-core/constants.ts#L256) (single-source change), cascading through [build.ts](../src/ui/conquest/hud-core/build.ts), [render.ts](../src/ui/conquest/hud-core/render.ts), [lifecycle.ts](../src/ui/conquest/hud-core/lifecycle.ts) | `twlConquestHudBuildShadowRingProfile` returns `[]`; every `Ensure`/`Render`/`Hide`/`Delete` consumer iterates the profile so empty-profile cascades to zero work without touching call sites. | **~280 widgets/pid reclaimed (~75% of M3 cache)**. Combat HUD widget count drops from ~372 to ~92/pid. At 16p: ~4,480 widget refs reclaimed. | −566 bytes (v1.442 → v1.443) | **Low** — visual regression on bright maps (text loses dark halo). Mitigation: chevron color inversion (v1.444) restores contrast on the most-affected surface; per-surface single-offset shadow restore from [`reference_implementations/reference_conquest_attempt_d/`](../reference_implementations/reference_conquest_attempt_d/) is the rollback path. | **SHIPPED v1.443; MP playtest validation pending — see [`conquest_mp_ongoing_tests.md`](./conquest_mp_ongoing_tests.md) Wave 6** |
| **A9** | **`maxPasses` 128/64 → 4 in pid-namespaced `safeFind` retry loops — Wave 6 Ship 0** | [ui/conquest/hud-core/lifecycle.ts:4](../src/ui/conquest/hud-core/lifecycle.ts#L4) (DOMINANT contributor — ~5,120 ops on disconnect pre-Wave-6), [index/player-join-leave.ts:19,39](../src/index/player-join-leave.ts#L19), [clock/timer-instance.ts:67](../src/clock/timer-instance.ts#L67) | Pid-namespaced widget IDs via `wn()` only produce duplicates if a prior cleanup was interrupted mid-loop. 4 passes is 4× tolerance for that edge case while cutting ~95% of `safeFind` ops on the dominant common path. | **No heap reclaim** (CPU-only). ~70% of disconnect-spike CPU reclaim. | ~0 | **Zero** — orphans are visibly observable; bump to 8 if SP/MP surfaces them. | **SHIPPED v1.443; MP playtest validation pending** |
| **A10** | **Stagger 3 join lazy-build triggers across 3 frames — Wave 6 Ship 1d** | [index/player-join-leave.ts:121-130](../src/index/player-join-leave.ts#L121) | `topHudShell` immediate, `vehicleDeployTimer` at `Timers.setTimeout(50ms)`, `combatHud` at `Timers.setTimeout(150ms)`. Distributes join cost across 3 frames at zero perceived UX cost (combat HUD not visible until first OnPlayerDeployed). | **No heap reclaim** (CPU-only — distributes the join hitch). | ~0 | **Low** — pid-validity race during deferred window mitigated by existing `triggerLazyBuild` guard at [lazy-build-registry.ts:236-237](../src/interaction/lazy-build-registry.ts#L236). | **SHIPPED v1.443; MP playtest validation pending** |
| ~~**A11**~~ | ~~**Plug `vehicleSpawnBaseTeamByObjId` leak on vehicle destroy (long-match audit L1)**~~ | ~~[config/runtime.ts:83], [index/vehicle-events.ts:106]~~ | **Superseded by v1.496 Tier C dead-code removal.** Post-ship grep confirmed the cache had ZERO read sites across `src/` — write-only dead code. v1.496 removed the cache declaration + spawn-side write + v1.495 destroy-side delete + `clearSpawnBaseTeamCache` helper + its lone game-mode-init caller entirely. The "leak" was a phantom: GC would have collected entries at game-mode init anyway since nothing depended on the values. | N/A — never a real leak | −361 bundle bytes (v1.495 → v1.496) | Zero (zero readers proved). | **SUPERSEDED by v1.496 Tier C dead-code removal.** Lesson: verify reader count before classifying a write site as a leak. |
| ~~**A12**~~ | ~~**Plug 3 module-local click trackers on player leave (long-match audit L3-L5)**~~ | [admin-panel/events.ts:7-11](../src/admin-panel/events.ts#L7), [interaction/ui-events-ready.ts:10-14](../src/interaction/ui-events-ready.ts#L10), [interaction/ui-events-player-ready-panel.ts:32-36](../src/interaction/ui-events-player-ready-panel.ts#L32); cleanup callsites in [index/player-join-leave.ts:77-79](../src/index/player-join-leave.ts#L77) | 3 exported `reset*PrimaryClickTrackerForPid(pid)` helpers (mirror of existing `resetVehicleDeployPrimaryClickTrackerForPid`) called from `cleanupHudForPid`. ASCII-only docblocks above each helper. | One number/pid per leaked tracker × 3 trackers; bounded at concurrent-pid count + accumulates across join/leave cycles. | folded into v1.500 +563 B total | **Zero post-fix.** v1.498 attempt broke via non-ASCII em-dash; v1.500 ships with pure ASCII + new postbuild guardrail catches future regressions. | **SHIPPED v1.500** (after v1.498 → v1.499 revert + root-cause investigation). |
| ~~**A13**~~ | ~~**Plug `lockerSlotToggle` on player leave (long-match audit L2)**~~ | [interaction/ammo-resupply-menu.ts:1254](../src/interaction/ammo-resupply-menu.ts#L1254) (write); cleanup line at [index/player-join-leave.ts](../src/index/player-join-leave.ts) per-pid delete cluster | `delete State.players.lockerSlotToggle[pid]` added to `onPlayerLeaveGameImpl` per-pid delete cluster next to other arm-* deletes. NO inline comment (the surrounding deletes are uncommented and self-documenting; v1.498 broke via an em-dash in the inline comment that was added). Rejoiner gets fresh `[2,2,2,2]` defaults per user direction 2026-05-09. | One small object per pid that ever opened locker, ~80 B per entry. | folded into v1.500 +563 B total | **Zero post-fix.** | **SHIPPED v1.500.** Lifecycle Map status flipped from ❌ to ✓. |

### Tier B — Module-level constant inlining (one-time but cumulatively large)

| # | Lever | Target | Mechanism | Heap impact | Bundle impact | Risk | Approval status |
|---|-------|--------|-----------|-----------|---------------|------|-----------------|
| B1 | Inline non-tunable layout constants — single-use UI pixel offsets that are never recomputed | `foundation/ui-layout.ts` (~308 const), `ui/conquest/hud-core/constants.ts` (~158 const), `interaction/ammo-resupply-menu.ts` (~67 const) | For each constant: if used at exactly one call site AND value never changes AND it's not a "tuning knob" name, inline the value at the call site. **Keep** any constant that (a) is used in 2+ places, (b) is named something a future maintainer would tune, or (c) represents a semantic meaning (`LOW_TIME_THRESHOLD_SECONDS`) | Removes module-level binding from each — at scale, hundreds of binding entries | Bundle bytes neutral or slightly + (loses the const name, gains the literal copy at each site) | **Medium-High** — inlining can hurt readability. Must keep "tuning intent" constants | **Critical user-discussion item** — user said: "A variable should exist because it needs to be modifiable due to some dynamic run time event, or some forward facing variable for tuning. Examples of this include accounting for a resolution change, or a movement in space." This tier directly applies. **Recommendation:** run a per-file pass and present a ~50-line "candidate for inlining" diff before any change |
| B2 | Collapse adjacent shadow-color triplets — `*_RGB: [number, number, number]` arrays + `mod.CreateVector(...)` derivatives | `foundation/ui-layout.ts` (~30 such pairs), `ui/conquest/hud-core/constants.ts` (similar) | Where both the `_RGB` tuple AND the `mod.CreateVector(...)` derivative are kept, drop whichever isn't read. Many sites only consume the `mod.CreateVector` form | Removes 30+ array literals × small | −small | Low — read-pattern audit suffices | Pending |
| B3 | Single-letter alias-of-alias (e.g. `const AX = -264; const EX = -88;` in [ammo-resupply-menu.ts:11-14](../src/interaction/ammo-resupply-menu.ts#L11)) | Various mega-files | Where a single-letter local already exists for the same value as a longer-named export, keep one | Removes duplicate bindings | −small | Low | Pending |

### Tier C — Dead code (confirmed-zero readers)

| # | Lever | Target | Mechanism | Heap impact | Bundle impact | Risk | Approval status |
|---|-------|--------|-----------|-----------|---------------|------|-----------------|
| C1 | Strings.json: 28 dead `joinPrompt.*` keys | `src/strings.json` | Delete keys; `FEATURE_JOIN_PROMPT = false` strips TS code but not strings — Cat 8.1 carry-over | ~5.2 KB strings.json reduction + matching runtime lookup table shrink | 0 (different cap) | Zero (gated feature off) | **User approval required** (string change policy in [AGENTS.md:75](../AGENTS.md#string-change-authorization-policy)) |
| C2 | Strings.json: ~30 other dead keys (Cat 8.2–8.7 carry-over) | `src/strings.json` | Categorized in prior analysis: 7 unused map names, 5 UI cache, 8 unused team-name combos (verify dropdown first), 7 readyDialog labels, 10 boundary/debug strings, 4 misc UI | ~3.5 KB strings.json reduction | 0 | Low (Cat 8.4 needs UI verification) | **User approval required** |
| **C3** | **Module-level `const` declarations whose values are referenced only inside their declaring file but never used** | `ui/conquest/hud-core/constants.ts` (162 consts audited) | Per-symbol grep across `src/` excluding the file itself; intra-file chain-tracing to find dead clusters | 9 consts deleted (2 truly dead + 7 SHADOW_OFFSET dead-chain) | **−1,311 bytes** | **Zero** — typecheck verified | **SHIPPED v1.470** — plan archive [`5.05.26_conquest_hud_core_constants_audit_plan.md`](./5.05.26_conquest_hud_core_constants_audit_plan.md). Sister-file audits for `foundation/ui-layout.ts` (the source of the 7 SHADOW_OFFSET pulls — may now also be dead) and `interaction/ammo-resupply-menu.ts` (~67 consts) deferred — separate plans if pursued. |
| C4 | `for...in` with `delete` in mega-files (Cat 5 item 1 carry-over) | Various | Each pass creates an iterator object. Replace with `for (const pid of Object.keys(record))` + filter pattern. (Already partially done; recheck remaining sites) | Iterator allocation reduction per call | 0 | Low | Pending |

### Tier D — Closure / continuation hygiene

| # | Lever | Target | Mechanism | Heap impact | Bundle impact | Risk | Approval status |
|---|-------|--------|-----------|-----------|---------------|------|-----------------|
| D1 | Audit `Timers.setTimeout` closure capture | Multiple files (`actions.ts`, `vanilla-spawner.ts`, `hq-deploy.ts`, etc.) | For each `Timers.setTimeout(() => {...})`, check what locals are captured. If the body needs only `pid`/`token`, bind those by extracting a named function and passing args explicitly. Captured locals like full `player` objects, large arrays, or VM snapshots can pin entire scopes | Variable; sites with heavy locals can release entire frames | −0 to small | Medium — refactor changes the readability of inline timer setup | Pending |
| D2 | `mod.Wait()` inside event handlers — same shape | gadget cooldown loops, supply-box warm scheduler, boundary live-prebuild scheduler | Same audit | Same shape | Same | Same | Pending (Wave 3 Ship 8 deleted the largest offender, `runLoadingGateUntilReady`) |
| D3 | Avoid `mod.AllPlayers()` materialization where iteration is short | `forEachValidPlayer` is the sanctioned wrapper. Verify all callers route through it | Already mostly done (`CQ_Refactor_forEachValidPlayer_Helper` #64) | Each `mod.AllPlayers()` allocates a Portal Array | 0 | Zero | Pending audit |

### Tier E — Opportunistic / readability (zero memory impact, zero functional change)

| # | Lever | Target | Notes |
|---|-------|--------|-------|
| E1 | Mega-file split — `capture-tickets.ts` (2,150 lines), `deploy-timer-ui.ts` (2,059 lines), `ammo-resupply-menu.ts` (2,769 lines) | 0 bundle, 0 heap. Pure readability/maintenance. Defer until reclaim ships. | |
| E2 | One-liner comment audit — replace multi-line block comments with single-line `//` per AGENTS.md "Function Comment Readability Policy". Postbuild strips full-line `//` cleanly (v1.397/v1.398). | 0 bundle (strip), positive readability | |
| E3 | Empty directory cleanup — `src/loaders/`, `src/team-switch/` are present but empty. Cosmetic. | 0 | |

### Tier F — Naming economy (modest memory, large code-clarity)

> **Re-measured v1.454, 2026-05-03.** Identifier statistics extracted via grep'ing `[A-Za-z_$][A-Za-z0-9_$]*` tokens from `dist/bundle.ts` and filtering JS keywords + common type names. Methodology may differ slightly from the v1.406 measurement (whose tool was unspecified) — both number sets are listed below. The v1.406-vs-v1.454 trend: total identifier text grew slightly despite F1's phase-prefix strip (Wave 3-6 added new helpers + lazy-build registry + admin slot state faster than F1 reclaimed bytes). See `conquest_optimization_state.md` "Naming Economy" section for the raw measurements and top-20 expensive identifiers.

| Metric | v1.454 (re-measured) | v1.406 (baseline) | Delta |
|--------|---------------------:|------------------:|-------|
| Identifier text bytes | **602,527** *(filtered)* | 577,898 | +24,629 (+4.3%) |
| Identifier % of bundle | **69.3%** *(filtered)* | 66.3% | +3.0pp |
| Unique identifiers | **5,858** *(filtered)* | 4,991 | +867 (+17.4%) |
| Identifier occurrences | **54,365** *(filtered)* | 51,626 | +2,739 (+5.3%) |
| Function declarations (`^function ` + `^async function `) | **951** | 1,027 | −76 (−7.4%) — F1 stripped 104 phase-prefix funcs at v1.408; offset by Wave 3-6 additions |
| Average function name length | **27.54 chars** | 28.6 chars | −1.06 chars (−3.7%) — F1's prefix-strip effect |
| Sum of function declaration name bytes | **26,190** | 128,566 *(call-site sum, different methodology)* | not directly comparable |

**Why this is its own tier.** Renaming doesn't compress the per-PID multiplier (Tier A target), it doesn't strip dead code (Tier C), it doesn't change scope (no-go list). What it does:

- **Bundle bytes:** direct savings, 4KB to 180KB depending on aggression level. Bundle has 176KB headroom currently, so this is *relief, not blocking*.
- **Runtime heap:** identifier strings get interned by the JS runtime. Savings is roughly `unique_name_count × avg_shorten` (paid once per unique name, not per call). Modest — likely 5–20KB heap relief depending on policy.
- **Code review hygiene:** large. Long phase-prefixed names hurt comprehension and pull-request review; this is the actual win.

**Phase-prefix anti-pattern.** 114 unique symbols carried `conquestPhase[2A|2B|3|4|4B]*` prefixes pre-v1.408. The prefixes described *when* the function was written (Phase 2A capture work, Phase 2B spawn-charge, Phase 3 HUD derivation, Phase 4/4B sound/VO queues), not *what* it did.

> **NOTE (v1.454 audit):** F1 SHIPPED at v1.408 — the rename is done. This section originally had a "before / after" example table here. The table is now stale because both columns auto-corrected to the post-rename names when F1's scripted rename pass ran. The actual diff lives in `src/Changelog.ts` v1.408 entry + git history before v1.408. Examples of the rename pattern, reconstructed from changelog narrative:
> - `conquestPhase3MarkHudDirty` → `markHudDirty` (16 calls)
> - `conquestPhase2BApplyBleedTick` → `applyBleedTick`
> - `conquestPhase2ASyncMappedCapturePointsFromEngine` → `syncMappedCapturePointsFromEngine`
> - `conquestPhase4FlushCaptureSoundQueue` → `flushCaptureSoundQueue`
> - `conquestPhase4BEnsureObjectiveVoState` → `ensureObjectiveVoState`
>
> Nine collisions during the rename were disambiguated via module-domain prefix: `spawnCharge*` (was Phase 2B), `captureSound*` (was Phase 4), `captureVo*` (was Phase 4B). One state variable (`conquestPhase2ACaptureTimingConfiguredByObjId`) was deliberately left out of scope.

Note: `lifecyclePhase` and `ConquestLifecyclePhase` describe a runtime *state value* ("we are in PRE_MATCH phase right now"), not implementation work — those stay.

| # | Lever | Bundle saved (est.) | Heap saved (est.) | Effort | Risk |
|---|-------|--------------------:|------------------:|--------|------|
| F1 | Strip `conquestPhase[2A|2B|3|4|4B]` prefix from 104 functions, leaving the descriptive remainder. **Resolved (v1.408, pending MP confirm — Wave 2).** Nine collisions disambiguated via module-domain prefix: `spawnCharge*` (Phase 2B), `captureSound*` (Phase 4), `captureVo*` (Phase 4B). One state variable (`conquestPhase2ACaptureTimingConfiguredByObjId`) deliberately out of scope per plan. | **−3,738 bytes** (measured v1.407 → v1.408) | ~1–2 KB | Shipped in ~1 hour via scripted rename + typecheck verification. | **Zero — pure rename.** | **Resolved (v1.408)** |
| F2 | F1 + cap remaining function names at 24 chars where the shorter form stays self-explanatory (e.g. `updateConquestCombatHudForAllPlayers` (36c) → `renderCombatHud` (15c) since `*ForAllPlayers` is implied by the codebase pattern) | ~24,000 bytes | ~6–10 KB | **1–2 days.** Per-symbol judgment call; needs naming review per file. ~700 functions evaluated, maybe 200–400 actually renamed. | **Medium.** Some renames change cognitive shape of code; need pull-request review. |
| F3 | F1 + F2 + audit shared variable names in mega-files (e.g. `ammoResupplyMenuCache` could be `armCache`, already done in some places — make consistent) | ~50,000 bytes | ~12–20 KB | **2–3 days.** Per-file pass over the four mega-files + state types. | **Medium.** Touches widely-imported names; large diff. |
| F4 | Aggressive cap-everything-at-16-chars rewrite | ~122,000 bytes | ~25–40 KB | **1+ week.** Pervasive churn across every file. | **High.** Names lose meaning; review burden enormous; high regression risk. **Not recommended.** |

**Recommendation:** ship F1 first. It's the highest-confidence-per-effort lever — pure cleanup of an actual anti-pattern, low risk, ~4 hour pass, makes future PRs cleaner. F2 piggybacks on F1 only if naming review during F1 surfaces obvious wins. F3 is conditional on F2 producing clean diffs. F4 is documented for completeness but is **not advised** — names are how humans read code, and squeezing them past readability for ~120KB bundle relief that we don't need is a bad trade.

**Naming standard going forward** (informs new code and any rename pass):

1. **Name for what, not when or why.** `markHudDirty`, not `markHudDirty`. `flushCaptureSoundQueue`, not `flushCaptureSoundQueue`. The phase context belongs in the changelog and module-header comment, not in the identifier.
2. **Keep names human-readable and intuitive.** `armCache` is fine; `arSpb_lck_chx_x` isn't. Reasonable shorthand is OK when it's *project conventional* (`pid` for player ID, `vid` for vehicle ID, `obj` for object) but never invent new shorthand for one-off use.
3. **Drop suffixes implied by context.** `*ForAllPlayers` adds no information when every renderer in the file iterates all players. Drop it where the file's intent is unambiguous.
4. **Reserve long names for unambiguous semantics.** If a function does something *exactly* as named by `safeSetUITextLabel`, that's a fine 18-char name — it tells you precisely what it does without surprise.
5. **Type-state names stay explicit.** `lifecyclePhase`, `ConquestLifecyclePhase`, `ConquestFlagVisualPhase` — these describe runtime state, not implementation phases. Keep them.

---

## Tier S — Per-frame CPU spike reclaim (v1.491 NEW; current active blocker)

> **Different budget.** Tiers A–F above target the **runtime heap budget** (16-player crash, v1.406). Tier S targets the **per-frame eval budget** — the engine's hard 1,000ms-per-frame cap, breached at 8–10 players in v1.491. Both budgets are real; the binding constraint shifted as the heap was reclaimed (Wave 6 brought heap pressure down enough that the per-frame work became the new ceiling).
>
> **A heap fix is not a CPU fix.** Removing widget refs (Tier A) reduces retained heap but does NOT reduce the per-frame work that builds them. A Tier S item targets **fan-out + frequency**, not allocation count.

### How to read a Tier S row

| Field | Meaning |
|-------|---------|
| **Frequency** | When does it fire? (per-event / per-tick / per-frame) |
| **Fan-out** | Per fire, how many player iterations? (typically O(N), sometimes O(N×F) or O(N²)) |
| **Throttle?** | Is there a cadence gate? Or unthrottled? |
| **Suspect rank** | S1 highest (most likely sole cause of v1.491 spike) → S10 lowest |
| **Severity** | 🔴 plausible 1716ms-class single-frame contributor, 🟡 compounding, 🟢 minor |

### Active Tier S table

> Source of truth for the per-row evidence: [`conquest_optimization_state.md`](./conquest_optimization_state.md) "Per-frame CPU fan-out — v1.491 crash hypothesis".

**Diagnostic toggle bundle shipped at v1.492-v1.494** lets the admin manually bisect S1/S2/S3 at MP playtest time:
- **Toggle Combat HUD** flips global `hudModeOverride = "off"` → short-circuits S1/S2 forced-broadcast hot path (per-player VM-build skipped at [`pipeline.ts:106-115`](../src/ui/conquest/hud-core/pipeline.ts#L106-L115)).
- **Toggle Vehicle Overlay** flips global `vehicleDeployTimerDisabledByAdmin` → short-circuits S3's `updateVehicleDeployTimerHudForAllPlayers` per-pid render-plan compute (only when player is walking around; spawn flows preserved).
- **Toggle Spectator** flips global `spectatorDisabledByAdmin` → kicks active spec + gates future entry.

These are **diagnostic** — they don't permanently fix the spike. After bisection identifies which subsystem is the dominant contributor, the actual reclaim ship (coalesce/throttle the broadcast in S1/S2/S3 row's "Reclaim direction" column) follows.

| # | Path | Frequency | Fan-out | Throttle? | Severity | Reclaim direction (proposed; needs user approval before any code change) |
|---|------|-----------|---------|-----------|:--------:|--------------------------------------------------------------------------|
| **S1** | `updateConquestCombatHudForAllPlayers(force=true)` from `OnPlayerEnterCapturePoint` | per enter event | O(N players × M VM-build) | **NONE — `force=true` bypasses 200ms gate** | 🔴 | **Coalesce.** 50ms `Timers.setTimeout` debouncer collapses cluster of N enters into 1 broadcast. Pattern: Wave 6 Ship 2 coalesced post-leave refresh. **User-approved 2026-05-09:** 50ms delay acceptable; user notes capture-storm explicit testing did not reproduce v1.491 spike, so S1 unlikely to be the systemic driver alone. |
| **S2** | `updateConquestCombatHudForAllPlayers(force=true)` from `OnPlayerExitCapturePoint` | per exit event | O(N players × M VM-build) | **NONE — bypasses gate** | 🔴 | Same coalescing as S1; share the debounce timer with enters since they bracket the same flag-cluster. **User-approved 2026-05-09 alongside S1.** |
| ~~**S3**~~ | ~~`updateVehicleDeployTimerHudForAllPlayers()` from `OnPlayerEnter/ExitVehicle`~~ | per vehicle event | O(N × 16 row sigs) | **200ms coalesce window (v1.497)** | 🔴 → ✅ | **SHIPPED v1.497** — broadcast wrapper now debounces via `Timers.setTimeout` 200ms drain. 26 call sites coalesce; latest state wins at drain time. Plan archive: [`5.09.26_conquest_S3_vehicle_deploy_timer_throttle_plan.md`](./5.09.26_conquest_S3_vehicle_deploy_timer_throttle_plan.md). MP playtest is the gate to confirm impact. |
| **S4** | `refreshReadyStatusForAllBuiltReadyDialogs` + `renderReadyDialogForAllVisibleViewers` from `OnPlayerExitAreaTrigger` (own main base) | per main-base trigger exit | 2 × O(built-dialog pids) | None | 🟡 | After Wave 4 Ship 3, only the admin pid has a built dialog → "all built" is usually 1 pid. Lower priority unless multiple admins (handoff residue) accumulate. |
| **S5** | `refreshLiveCaptureStateSubtick` (`syncMappedCapturePointsFromEngine`) | every 120ms | O(F flags × `mod.GetPlayersOnPoint`) | Subtick interval IS the throttle | 🟡 | Steady baseline. Lower priority unless eval profile shows the per-flag `GetPlayersOnPoint` is itself slow. |
| **S6** | `onLiveTick` per-second: `applyBleedTick` + `updateConquestCombatHudForAllPlayers()` (no force) + `updateVehicleDeployTimerHudForAllPlayers()` | per second | mixed | combat HUD throttled; vehicle HUD not | 🟡 | Vehicle HUD broadcast at 1Hz is unnecessary if no vehicle slot state changed — gate on a per-tick `vehicleSlotsDirty` flag. |
| **S7** | LIVE-transition burst: 9 N-player broadcasts in `startMatch` + sync batch-0 of `BoundaryPromptLivePrebuildScheduler` | once per match | 9 × O(N) + 1 × O(min(N,8) × 12 widgets) | None | 🟢 | Already documented as a separate "slight hitch at LIVE" by user. Stagger pattern (Wave 6 Ship 1d) applies. Make `BoundaryPromptLivePrebuildScheduler` batch-0 fire at `delayMs = BATCH_INTERVAL_MS` instead of `0`. |
| **S8** | Late-joiner during LIVE: `triggerLazyBuild('vehicleDeployTimer', joinPid)` at +50ms; sync slot-render chain | per late join | O(1) per join | Wave 6 Ship 1d staggers vs other surfaces but doesn't coalesce concurrent joins | 🟡 | Deduplicate concurrent join builds; if 2 players join within 100ms, share the build pass. |
| **S9** | `findNearestDeployedTeammatePid` (squad-spawn inheritance) at deploy storm | per deploy | O(N forEachValidPlayer scan) | None | 🟢 | At LIVE start, 10 players deploying within seconds = 10 × O(N) scans. Cap at deploy cadence so doesn't spike per-frame; lower priority. |
| **S10** | Boundary classifier safety-net re-probe (chopper passengers flagged OOB) | per OOB classify | O(misclassify rate × N) | None | 🟡 | **Original hypothesis invalidated 2026-05-09** — user confirmed via position-debug tool that helicopter passengers ARE flagged in-vehicle by the engine. Misclassification source is upstream of `IsInVehicle`, not a SoldierStateBool gap. Revised investigation needed: order-of-operations between vehicle attribution and altitude-vs-ceiling check, vehicle-type filter (passenger ceiling vs host vehicle ceiling), or stale per-player altitude-cap state. **Tier S row stays 🟡 but the reclaim direction is unknown until upstream is identified.** |

### How Tier S relates to Tier A (heap)

The two budgets are **mostly independent**:

- A Tier A reclaim (e.g. removing 5 widget refs/pid in M3) reduces retained heap but does **not** reduce the per-frame VM build cost — the work that touches those widgets still runs.
- A Tier S reclaim (e.g. throttling vehicle-event broadcasts) reduces per-frame fan-out but does **not** change the steady-state heap size.

A few items overlap:
- **S1+S2 coalescing** also reduces churn allocations (M11 territory) since fewer VM rebuilds run per second. Marginal heap impact, real CPU impact.
- **S3 throttling** would let M1 (`vehicleDeployTimerCache`) receive fewer redundant `mod.SetUI*` writes per second — slight engine-side relief.

The recommended sequencing post-v1.491:
1. **Tier S first** (active blocker — script crashes within minutes of LIVE at 8–10p).
2. Resume Tier A work after stability is restored.
3. Tier B/C/D/E/F continue alongside Tier A on best-effort cadence.

### Verification protocol for Tier S items

The crash signature is the verification surface:

1. Apply one Tier S item (start with S1).
2. `npm run bumpVersion` + ship.
3. 8–10 player MP playtest, hold for 10+ minutes of active LIVE play with capture-fight clusters.
4. **Pass criteria:** no `1,000ms exceeded` engine error.
5. **Fail criteria:** error fires; capture timestamp + connected pid count + last broadcast event class. Bisect.

The runtime budget is opaque (no `mod.GetFrameTimeMs` or similar — verified against [`reference_sdk_1.2.3`](../../reference_sdk_1.2.3) — same as the heap budget); progress measured only by "does the script terminate?". Bisect by tier accordingly. **Suggest one S item per playtest cycle** so attribution is clear.

### Open questions for user (Tier S) — answered 2026-05-09

1. ~~**S1/S2 — is "atomic enter/exit visual flip" load-bearing?**~~ **Answered 2026-05-09.** User: 50ms coalesce delay is fine if it gives meaningful gains. Caveat: user doubts S1/S2 is the systemic v1.491 driver based on testing (capture-storm explicit testing did not reproduce the spike). **Implication:** S1/S2 coalescing is approved as a reclaim ship, but should not be the sole-bet fix for v1.491 — likely needs to be combined with S3 throttling and/or the long-match accumulation fixes (A11) to actually move the spike pattern.
2. ~~**S3 — vehicle-event broadcast throttling.**~~ **Answered 2026-05-09.** User: vehicle deploy timer does not need hyper-accurate frame-coincident updates; can catch up within reason. **Implication:** S3 throttling (200ms gate or per-tick `vehicleSlotsDirty` flag) is approved.
3. ~~**S10 — vehicle attribution via `IsInVehicle` SoldierStateBool.**~~ **Original hypothesis invalidated 2026-05-09.** User confirmed via position-debug tool: player IS flagged as in-vehicle when seated in a helicopter. So the boundary misclassification driving false-OOB on chopper passengers is **NOT** a `safeGetSoldierStateBool(IsInVehicle)` gap. **Revised hypothesis:** the misclassification source is upstream of (or independent of) the IsInVehicle check — likely either (a) the boundary classifier's order-of-operations between vehicle attribution and altitude-vs-ceiling logic, (b) a vehicle-type filter that rejects helicopters (passengers carrying their own ceiling expectation while the host vehicle's ceiling is honored), or (c) a per-player altitude-cap state that's stale relative to the current host vehicle. Needs new investigation pass against the boundary enforcement code path before any Tier S fix is proposed for S10.

---

## Justification rules (what counts as "necessary")

The user's bar: *"A variable should exist because it needs to be modifiable due to some dynamic run time event, or some forward facing variable for tuning. Examples of this include accounting for a resolution change, or a movement in space."*

Operationalized rules used to classify candidates above:

1. **Keep** if reassigned at runtime (`let` mutated by an event handler, or a `const` whose object body is mutated).
2. **Keep** if read at 2+ call sites with a meaningful name carrying tuning intent (e.g., `LOW_TIME_THRESHOLD_SECONDS`, `CONQUEST_BLEED_PER_DIFF_PER_SECOND`).
3. **Keep** if the value depends on a runtime input — resolution, spatial position, player count, map config.
4. **Keep** if the name documents a non-obvious magic number (semantic clarity).
5. **Inline** if the value is single-use, immutable, and the literal at the use site reads no worse than the named binding.
6. **Inline** if multiple constants are derivatives of one parent and the parent is the actual tunable (e.g. layout-derived offsets — keep parent, inline derivatives).
7. **Delete** if the symbol has zero readers anywhere in `src/`.

Applying these to `foundation/ui-layout.ts` as a sample: many `*_OFFSET_X`, `*_OFFSET_Y`, `*_PADDING` constants are used at exactly one site and never tuned in 200+ versions. Those are inline candidates. Color triplets and the explicit `LOW_TIME_THRESHOLD_SECONDS` are keepers — they are tuning knobs.

---

## Why per-PID UI is non-negotiable

The temptation when looking at the per-player multiplier table is to ask "what if we made it per-team?" — that would collapse 16 widget trees to 2 and solve the problem. **It can't be done.** Per-PID UI is locked in by two principles:

1. **Interactivity.** When a player hovers, focuses, or clicks a widget, the visual reaction is for that player only. Team- or globally-scoped widgets cause cross-player visual clashes — one player's hover state lights up the button for everyone, one player's focus indicator is everyone's. Gameplay UX breaks.
2. **Responsivity.** When a player connects (initial join, late join, team swap, reconnect), the UI state must be contextual to **them** — their team perspective coloring, their cooldowns, their button-enable state, their engage HUD, their boundary prompts. Without PID scope, a late-joiner inherits whatever stale shared state existed and can't get a contextual view of the match.

These two principles produce the architecture as observed: PID-suffixed widget names (`wn(name, pid)`), per-PID generation tokens for stale-ref invalidation (`combatHudGenerationByPid`), per-PID lazy-build dispatch with per-surface in-flight guards (`triggerLazyBuild(name, pid)` post-Wave-3), per-PID destroy-rebuild for recovery, per-PID-scoped HUD ownership added in `CQ_Bug_9` to fix cross-player clash. None of those are over-engineering — they fall out of the two principles above.

**The implication for memory reclaim:** the per-PID heap multiplier at 16 players is the **inherent cost of doing the UI right**, not waste. The reclaim plan must trim **inside** the per-PID model — drop dead fields, consolidate fragmented small allocations into single parents, remove diff-cache mirrors that the engine can absorb cheaply — not try to widen scope.

The one possible exception is the match clock (M5): it's purely passive, has no interactive elements, and its content is genuinely identical for all viewers. *Maybe* a candidate for global scope, but only if the late-joiner contextual-state path stays correct under team-swap and reconnect. Worth investigating but not assumed.

Everything else stays per-PID. Reclaim works within that constraint.

## Verified safe operations (explicit no-go list)

The reclaim pass must NOT:

- Change UI look (positions, colors, sizes, animations, render cadence visible to the player).
- Change feature behavior (every gameplay event continues to fire identically).
- Modify `src/strings.json` without explicit user approval per [AGENTS.md:75](../AGENTS.md#string-change-authorization-policy).
- Re-introduce `mod.AddUIIcon` (non-functional per [AGENTS.md:58](../AGENTS.md#mod.addUIIcon-is-non-functional)).
- Bypass the dirty-flag HUD contract ([AGENTS.md:139](../AGENTS.md#combat-hud-dirty-flag-contract)).
- Re-introduce pre-seat `mod.Teleport(player, ...)` ([memory: project_teleport_vehicle_spawn_mystery.md](../../../../.claude/projects/c--Users-Soldat-TypeScriptProjects-twlmain/memory/project_teleport_vehicle_spawn_mystery.md)).
- Touch the v1.259 vanilla-spawner architecture (single-persistent-spawner + spawnMutex).
- Skip `bumpVersion` when shipping a code change.
- Increase per-player allocations for any reason short of a user-approved feature add.
- **Convert any interactive or responsivity-dependent widget surface from per-PID to per-team or global scope.** See "Why per-PID UI is non-negotiable" above. The match clock is a possible narrow exception; everything else stays per-PID.

---

## Open questions for user (per `Don't delegate understanding` policy) — answered 2026-05-09

1. ~~**Inline-vs-keep policy for layout constants.**~~ **Answered 2026-05-09.** User: keep constants only when they need to be dynamic (modifiable at runtime due to a real event) or are forward-facing tuning knobs. Everything else inlines. This matches the existing "Justification rules" section (rules 1–4 keep, 5–7 inline/delete). **Implication:** Tier B1 ships as **option (a) aggressive inline pass**, governed by the existing rules — reassigned-at-runtime keepers, 2+-call-site keepers with tuning intent (`LOW_TIME_THRESHOLD_SECONDS` shape), and runtime-input-derived keepers (resolution, spatial position, player count, map config) survive. Single-use derivative offsets without tuning intent inline. Per-file pass with diff preview before each ship.

2. ~~**Diff-cache fields (Tier A2) — perf-load-bearing test.**~~ **Answered 2026-05-09.** User: not effectively measurable without telemetry we don't have. **Implication:** A2 stays unshipped. Keep all `last*` diff-cache fields conservatively until (a) we get engine-side write-cost telemetry, (b) a diff cache is proven dead by other audit (zero readers), or (c) a refactor consolidates them as a side-effect. Do not remove them speculatively.

3. ~~**`uiCachePerfByPid` (M13) — keep or strip?**~~ **Answered 2026-05-09.** User: strip later, low priority unless it's in the way or causing issues. **Implication:** A5 stays parked. Pick up only as a side-effect of a related refactor or if `FEATURE_PERF_DIAG` is permanently abandoned.

4. ~~**Strings.json dead-key cleanup (Tier C1, C2).**~~ **Answered 2026-05-09.** User: removable if provably unused. **Implication:** ship C1 (28 `joinPrompt.*` keys, gated feature off) and C2 (~30 misc dead keys) after a fresh grep pass per category to prove zero readers. Present a pre-edit diff per category before deletion ship to confirm "provably not using these" — categories like Cat 8.4 (unused team-name combos) need a UI dropdown re-verification first.

5. ~~**Per-player consolidation (Tier A3) — now or later?**~~ **Answered 2026-05-09.** User: depends on priority and impact (asking for assessment). **Assessment:** Impact is small — A3 saves 6×16=96 small allocations + 6 record headers per match, but the underlying field count is unchanged. M10 is already in the S-bucket (per-player heap is already small). Risk is low (mechanical), but touches ~30 read sites in `ammo-resupply-menu.ts`. Priority is low compared to Tier S (active blocker) and the A11–A13 leak fixes (mechanical, smaller diffs). **Recommendation:** defer A3 until either a menu rewrite happens organically or Tier S is fully resolved and we cycle back to Tier A polish work. Not a near-term ship.

6. ~~**A13 — `lockerSlotToggle` reset-on-leave (long-match audit L2).**~~ **Answered 2026-05-09.** User: server cannot associate a rejoiner with their previous preference, so just use defaults. **Implication:** A13 approved. Ship `delete State.players.lockerSlotToggle[pid]` on player leave; rejoiner gets fresh `[2,2,2,2]` (GadgetTwo per class). Update Lifecycle Map status from ❌ to ✓ after the ship lands.

---

## Verification plan after reclaim ships

The 16-player playtest is the single source of truth. No engine telemetry exists for the heap budget.

1. Apply approved Tier A items first (highest ROI per player, lowest risk).
2. `npm run bumpVersion -- -c "<entry>"` and `npm run build`. Confirm bundle still under cap.
3. Run a 16-player playtest. **Pass criteria:** match starts to victory dialog without script termination.
4. If pass: continue with Tier B–E approved items, plus Tier F1 (phase-prefix strip) which is independent and low-risk.
5. If fail: capture termination time + connected pid count. Implement next-tier reclaim. Re-test.

The runtime budget is opaque, so progress is measured only by "does the script terminate?" — bisect by tier accordingly.

---

## Carry-forward notes (still accurate from prior analysis)

These items remain unchanged from the v1.390 baseline; collected here so the prior context stays accessible without re-reading the historical version.

- **Bundle vs strings — two separate caps.** `dist/bundle.ts` is governed by the 1,048,576-byte cap. `dist/bundle.strings.json` is **not** counted against that cap. `FEATURE_*` flags do NOT gate strings. Cleaning dead strings reduces runtime memory, not bundle pressure.
- **Match clock self-drives** via `Clocks.CountDownClock` since v1.337/v1.338. Not a hot path.
- **AreaTriggers require explicit `mod.EnableAreaTrigger(t, true)`** at game-mode start ([CQ_Feat_AreaTrigger_Enable, v1.367](./conquest_issues_summary.md)). Same lesson generalizes to other engine-object enables.
- **TickContext shares `mod.AllPlayers()` per subtick** ([CQ_Perf_TickContext_AllPlayers_Cache, v1.220](./conquest_issues_summary.md)). One open follow-up: `pipeline.ts:125` raw `mod.AllPlayers()` should consume the active TickContext.
- **Combat HUD dirty-flag contract** ([AGENTS.md](../AGENTS.md#combat-hud-dirty-flag-contract)) — every state mutation that affects HUD must call `markHudDirty()`.
- **Engine event reliability is asymmetric** — `OnPlayerEnterVehicle` has known gaps (CQ_Bug_43, #106 v1.383); `OnPlayerExitVehicle` is reliable.
- **`ForcePlayerToSeat` is only reliable inside `OnPlayerDeployed`** (Phase 6 HQ Deploy contract).
- **`SetObjectTransform` is no-op on `Vehicle`** — every post-bind vehicle placement is `mod.Teleport`.
- **`SetObjectTransform` on a persistent `VehicleSpawner` does not propagate at altitude** (v1.331 probe).

---

## File map for reclaim work

The reclaim ladder will touch (in approximate order):

- [state/runtime-types.ts](../src/state/runtime-types.ts) — Tier A1, A4, A5
- [state/runtime-state.ts](../src/state/runtime-state.ts) — Tier A1, A3, A4, A5 init parity
- [state/hud-cache-types.ts](../src/state/hud-cache-types.ts) — Tier A2 (after profiling decision)
- [vehicles/vanilla-spawner.ts](../src/vehicles/vanilla-spawner.ts) — Tier A1 init drop
- [interaction/ammo-resupply-menu.ts](../src/interaction/ammo-resupply-menu.ts) — Tier A3, B1 sample, B3
- [vehicles/deploy-timer-ui.ts](../src/vehicles/deploy-timer-ui.ts) — Tier A2 (largest target)
- [foundation/ui-layout.ts](../src/foundation/ui-layout.ts) — Tier B1
- [ui/conquest/hud-core/constants.ts](../src/ui/conquest/hud-core/constants.ts) — Tier B1
- [src/strings.json](../src/strings.json) — Tier C1, C2 (after approval)
- All `Timers.setTimeout` / `mod.Wait` sites — Tier D audit

Each file change needs the AGENTS.md "Change Log and Versioning Policy" workflow: `npm run bumpVersion -- -c "<entry>"` followed by `npm run build` and `cmd /c npx tsc --pretty false --noEmit`. Bundle-size line in handoff per AGENTS.md "Output Requirements".
